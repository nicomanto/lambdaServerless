service: helloWorld


frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: 20201221
  region: eu-south-1

functions:
  currentTime:
    handler: handler.currentTime
    events:
      - http:
          path: current-time
          method: get
